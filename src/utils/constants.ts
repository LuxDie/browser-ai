/**
 * Ruta del archivo HTML del sidepanel.
 */
export const SIDEPANEL_PATH = 'sidepanel.html';

/**
 * Ruta del icono principal de la extensión (128px).
 */
export const EXTENSION_ICON_PATH = 'icons/icon-128.png';

/**
 * Mensaje estándar de error cuando falla la conexión entre componentes.
 * Se utiliza para capturar el error relacionado con sidepanel no completamente cargado.
 */
export const CONNECTION_ERROR_MESSAGE = "Could not establish connection. Receiving end does not exist.";